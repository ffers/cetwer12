[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "asxcrm"
version = "0.0.1"

[tool.pytest.ini_options]
pythonpath = [
  "."
]
addopts = [
    "--import-mode=importlib",
]